{
  "rules": {
    ".read": false,
    ".write": false,
    "rideRequests": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "conductores_activos": {
      ".read": "auth != null",
      "$driverId": {
        ".write": "auth != null && (auth.uid == $driverId || true)"
      }
    }
  }
}
